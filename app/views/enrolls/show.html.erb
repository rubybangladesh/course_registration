<p>
  <strong>User:</strong>
  <%= @enroll.user.first_name %>
</p>

<p>
  <strong>Courses:</strong>
  <% @enroll.registers.each do |r| %>
   <%= r.course.course_title %> |
  <% end %>
</p>

<p>
  <strong>Semester:</strong>
  <%= @enroll.semester.sem_name %>
</p>

<%= link_to 'Edit', edit_enroll_path(@enroll), class: "btn btn-primary" %> |
<%= link_to 'Back', enrolls_path, class: "btn btn-primary" %>
